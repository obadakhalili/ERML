{
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "yarn run prettier --write .",
    "format:all": "yarn run prettier --ignore-path .gitignore --write ."
  },
  "devDependencies": {
    "@types/jest": "26.0.23",
    "babel-jest": "^27.0.2",
    "jest": "27.0.4",
    "prettier": "2.3.1",
    "rimraf": "3.0.2",
    "ts-jest": "27.0.3",
    "typescript": "4.3.2"
  }
}
