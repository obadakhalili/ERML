{
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "yarn run prettier --write .",
    "format:all": "yarn run prettier --ignore-path .gitignore --write ."
  },
  "devDependencies": {
    "@types/node": "^15.12.2",
    "cross-env": "^7.0.3",
    "prettier": "^2.3.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.2"
  }
}
